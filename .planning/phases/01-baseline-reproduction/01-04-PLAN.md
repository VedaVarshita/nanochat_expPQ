---
phase: 01-baseline-reproduction
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - veda/logs/run2.txt
  - veda/docs/BASELINE.md
autonomous: true
requirements:
  - REPRO-01
  - REPRO-02
  - EVAL-03

must_haves:
  truths:
    - "Second training run (run2) completes with same config as run1"
    - "Run2 val_bpb values are within +/-0.005 of run1 at every checkpoint"
    - "BASELINE.md contains complete BPB curve table (step -> val_bpb for all eval points)"
    - "BASELINE.md contains CORE score, experiment protocol, torch.compile outcome"
    - "Experiment protocol records: seed, config flags, data version, tokenizer checkpoint hash"
  artifacts:
    - path: "veda/logs/run2.txt"
      provides: "Complete training log from reproducibility run"
    - path: "veda/docs/BASELINE.md"
      provides: "Permanent baseline reference document"
      contains: "## BPB Curve"
  key_links:
    - from: "veda/logs/run1.txt"
      to: "veda/docs/BASELINE.md"
      via: "grep val_bpb extraction"
      pattern: "Validation bpb"
    - from: "veda/logs/run2.txt"
      to: "veda/docs/BASELINE.md"
      via: "reproducibility comparison"
      pattern: "Validation bpb"
---

<objective>
Run the reproducibility training run (run2), verify val_bpb matches run1 within +/-0.005, and compile all results into veda/docs/BASELINE.md.

Purpose: Prove the baseline is reproducible (REPRO-01) and create the permanent reference document (EVAL-03) that all Phase 2 and Phase 3 experiments will be compared against. This is the culminating deliverable of Phase 1.
Output: veda/logs/run2.txt with second training log, veda/docs/BASELINE.md with BPB table, CORE score, experiment protocol, and reproducibility verification.
</objective>

<execution_context>
@/Users/vedavarshita/Desktop/Git-repos/nanochat_expPQ/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedavarshita/Desktop/Git-repos/nanochat_expPQ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-reproduction/01-CONTEXT.md
@.planning/phases/01-baseline-reproduction/01-RESEARCH.md
@runs/runcpu.sh

@.planning/phases/01-baseline-reproduction/01-01-SUMMARY.md
@.planning/phases/01-baseline-reproduction/01-02-SUMMARY.md
@.planning/phases/01-baseline-reproduction/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute reproducibility run (run2)</name>
  <files>veda/logs/run2.txt</files>
  <read_first>
    - runs/runcpu.sh (exact same flags as run1)
    - .planning/phases/01-baseline-reproduction/01-RESEARCH.md (Pitfall 6 — run2 must NOT retrain tokenizer)
    - .planning/phases/01-baseline-reproduction/01-CONTEXT.md (D-06 — run2 log goes to veda/logs/run2.txt)
    - veda/logs/run1.txt (first 5 lines — confirm format of existing log)
  </read_first>
  <action>
**CRITICAL: Do NOT retrain the tokenizer.** Run2 executes ONLY base_train (not dataset download, not tok_train). The tokenizer checkpoint from run1 is reused exactly as-is (per TOK-04, Pitfall 6).

Before starting, delete the existing checkpoint so the model trains fresh (not resumes):
```bash
rm -rf ~/.cache/nanochat/base_checkpoints/d6/
```

Run the exact same base_train command as run1, logging to run2.txt (per D-06):
```bash
source .venv/bin/activate && \
WANDB_RUN=dummy python -m scripts.base_train \
    --depth=6 \
    --head-dim=64 \
    --window-pattern=L \
    --max-seq-len=512 \
    --device-batch-size=32 \
    --total-batch-size=16384 \
    --eval-every=100 \
    --eval-tokens=524288 \
    --core-metric-every=-1 \
    --sample-every=100 \
    --num-iterations=5000 \
    --run=dummy \
    2>&1 | tee veda/logs/run2.txt
```

This takes ~30 minutes. Do not interrupt at step 0 (Metal warmup).

After completion, verify val_bpb count:
```bash
grep -c "Validation bpb" veda/logs/run2.txt
```
Expected: 51 lines (same as run1).

**Reproducibility check (REPRO-01):**
Compare val_bpb values between run1 and run2:
```bash
paste <(grep "Validation bpb" veda/logs/run1.txt | awk '{print $NF}') \
      <(grep "Validation bpb" veda/logs/run2.txt | awk '{print $NF}') | \
  awk '{diff=$1-$2; if(diff<0) diff=-diff; if(diff>0.005) print "FAIL step " NR ": delta=" diff; else print "OK step " NR ": delta=" diff}'
```

All lines must show "OK" (delta <= 0.005). If any show "FAIL", record which steps diverged.
  </action>
  <verify>
    <automated>grep -c "Validation bpb" veda/logs/run2.txt && echo "run2 val_bpb count OK"</automated>
  </verify>
  <acceptance_criteria>
    - `veda/logs/run2.txt` exists and is non-empty
    - `grep -c "Validation bpb" veda/logs/run2.txt` returns 51 (or same count as run1)
    - `grep "Step 05000" veda/logs/run2.txt` returns at least 1 match
    - Reproducibility paste/awk check shows all deltas <= 0.005
  </acceptance_criteria>
  <done>
    - Run2 completed 5000 steps with same config as run1 (REPRO-01)
    - val_bpb values within +/-0.005 of run1 at all checkpoints (REPRO-01)
    - Run2 log saved to veda/logs/run2.txt (D-06)
    - Tokenizer was NOT retrained — reused from run1 (TOK-04)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create BASELINE.md with all results</name>
  <files>veda/docs/BASELINE.md</files>
  <read_first>
    - .planning/phases/01-baseline-reproduction/01-CONTEXT.md (D-01 through D-07 — all BASELINE.md decisions)
    - veda/logs/run1.txt (grep "Validation bpb" to extract BPB values)
    - veda/logs/run2.txt (grep "Validation bpb" to extract run2 BPB values)
    - veda/logs/eval1.txt (grep "CORE" to extract CORE score)
    - runs/runcpu.sh (exact config flags for experiment protocol)
  </read_first>
  <action>
Create `veda/docs/BASELINE.md` containing ALL of the following sections (per D-01, D-02, D-03):

**Section 1: Header**
```markdown
# Baseline Reproduction Results

**Date:** {today's date}
**Hardware:** Apple Silicon MPS
**Config:** runcpu.sh (depth=6, head-dim=64, seq-len=512, batch=32, 5000 iterations)
```

**Section 2: BPB Curve (per D-02)**

A markdown table with columns: `Step | Run 1 val_bpb | Run 2 val_bpb | Delta`

Extract values from log files:
```bash
grep "Validation bpb" veda/logs/run1.txt | awk '{print $2, $NF}'
grep "Validation bpb" veda/logs/run2.txt | awk '{print $2, $NF}'
```

The table should have one row per eval step (step 0, 100, 200, ..., 5000). Include all rows — do not truncate. Format:
```markdown
## BPB Curve

| Step | Run 1 val_bpb | Run 2 val_bpb | Delta |
|------|---------------|---------------|-------|
| 00000 | X.XXXXXX | X.XXXXXX | +/-X.XXXXXX |
| 00100 | X.XXXXXX | X.XXXXXX | +/-X.XXXXXX |
...
| 05000 | X.XXXXXX | X.XXXXXX | +/-X.XXXXXX |
```

**Section 3: CORE Score (per D-03)**
```markdown
## CORE Score

DCLM CORE metric: {value from eval1.txt}
CSV path: ~/.cache/nanochat/base_eval/{filename}.csv
```

**Section 4: torch.compile Outcome (per D-03)**

Extract from run1.txt:
```bash
grep -i "compile\|eager\|fallback\|inductor" veda/logs/run1.txt | head -20
```

Document what happened:
```markdown
## torch.compile Outcome

{SUCCESS: both adamw_step_fused and muon_step_fused compiled successfully on MPS}
{OR FALLBACK: fell back to eager mode with warning: {warning text}}

Relevant log lines:
{paste the grep output}
```

**Section 5: Experiment Protocol (per D-03, REPRO-02)**
```markdown
## Experiment Protocol

- **Seed:** {from base_train.py — torch.manual_seed(42) in compute_init}
- **Config flags:** depth=6, head-dim=64, window-pattern=L, max-seq-len=512, device-batch-size=32, total-batch-size=16384, eval-every=100, eval-tokens=524288, core-metric-every=-1, sample-every=100, num-iterations=5000
- **Data version:** nanochat.dataset -n 8 (~2B chars from HuggingFace)
- **Tokenizer checkpoint hash:** {sha256 from Plan 01 — run shasum -a 256 ~/.cache/nanochat/tokenizer.model}
- **PyTorch version:** {from python -c "import torch; print(torch.__version__)"}
- **Git commit:** {from git rev-parse HEAD}
```

**Section 6: Reproducibility (REPRO-01)**
```markdown
## Reproducibility

Max val_bpb delta between Run 1 and Run 2: {max delta value}
Threshold: +/-0.005
Result: {PASS or FAIL}
```

To compute values, run the extraction commands above and populate the table with actual numbers from the log files. Do NOT use placeholder values — every number must come from the actual log files.
  </action>
  <verify>
    <automated>grep "## BPB Curve" veda/docs/BASELINE.md && grep "## CORE Score" veda/docs/BASELINE.md && grep "## Experiment Protocol" veda/docs/BASELINE.md && grep "## Reproducibility" veda/docs/BASELINE.md && echo "BASELINE structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - `veda/docs/BASELINE.md` exists
    - `grep -c "|" veda/docs/BASELINE.md` returns at least 55 (table header + 51 data rows + section headers)
    - `grep "## BPB Curve" veda/docs/BASELINE.md` returns 1 match
    - `grep "## CORE Score" veda/docs/BASELINE.md` returns 1 match
    - `grep "## torch.compile" veda/docs/BASELINE.md` returns 1 match
    - `grep "## Experiment Protocol" veda/docs/BASELINE.md` returns 1 match
    - `grep "## Reproducibility" veda/docs/BASELINE.md` returns 1 match
    - `grep "Tokenizer checkpoint hash" veda/docs/BASELINE.md` returns 1 match with an actual sha256 hash
    - `grep "PASS\|FAIL" veda/docs/BASELINE.md` returns at least 1 match in the Reproducibility section
    - No placeholder values like "X.XXXXXX" remain in the file — all values are actual numbers
  </acceptance_criteria>
  <done>
    - BASELINE.md created at veda/docs/BASELINE.md (D-01)
    - BPB curve table with step -> val_bpb for all eval points (D-02, EVAL-03)
    - CORE score documented (D-03, EVAL-03)
    - torch.compile outcome documented (D-03)
    - Experiment protocol with seed, flags, data version, tokenizer hash (D-03, REPRO-02)
    - Reproducibility result documented with max delta (REPRO-01)
    - Baseline reference is complete and ready for Phase 2 and Phase 3 comparisons (EVAL-03)
  </done>
</task>

</tasks>

<verification>
- `cat veda/docs/BASELINE.md` shows complete document with all sections
- BPB table has 51+ data rows with actual numeric values
- CORE score is a real number (not placeholder)
- Experiment protocol has all required fields filled
- Reproducibility section shows PASS/FAIL with actual max delta
</verification>

<success_criteria>
veda/docs/BASELINE.md exists with complete BPB curve table, CORE score, experiment protocol, torch.compile outcome, and reproducibility verification. All values are actual numbers extracted from training logs. This document is the permanent baseline reference for all future experiments.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-reproduction/01-04-SUMMARY.md`
</output>
